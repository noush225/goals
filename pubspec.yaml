name: goals
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  sqflite: ^2.3.3+1
  path: ^1.9.0
  path_provider: ^2.1.3
  google_fonts: ^6.2.1
  provider: ^6.1.2
  audio_service: ^0.18.17
  just_audio: ^0.9.42
  audio_session: ^0.1.21
  cupertino_icons: ^1.0.8
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/audio/drawingsample1.mp3
    - assets/audio/drawingsample2.mp3
